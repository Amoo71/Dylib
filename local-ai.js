export function generateLocalPlan(request,stock){const q=request.toLowerCase(),steps=[];const add=(size,x,y,color='#d71920',z=0,rotation=0)=>{if(steps.filter(p=>p.size===size).length<(Number(stock[size])||0))steps.push({size,x,y,z,rotation,color})};if(/auto|wagen|car/.test(q)){for(let i=0;i<Math.min(4,Number(stock['2x4'])||0);i++)add('2x4',4+i*2,7);for(let i=0;i<Math.min(4,Number(stock['1x1'])||0);i++)add('1x1',5+i%2,6+Math.floor(i/2),'#171717');return{name:'Kleines Auto',steps}}if(/baum|tree/.test(q)){for(let i=0;i<Math.min(3,Number(stock['1x2'])||0);i++)add('1x2',7,8+i,'#8b4513');for(let i=0;i<Math.min(5,Number(stock['1x1'])||0);i++)add('1x1',6+i%3,6+Math.floor(i/3),'#16a34a');return{name:'Kleiner Baum',steps}}for(let y=0;y<2;y++)for(let x=0;x<4;x++)add('2x4',4+x*2,5+y*2);return{name:/haus|house|gebäude|building/.test(q)?'Kleines Haus':'LEGO-Modell',steps}}